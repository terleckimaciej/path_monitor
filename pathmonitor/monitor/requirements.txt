
# --- CORE ---
watchdog==4.0.1         # monitorowanie FSwatchdog==4.0.1         # monitorowanie FS (Observer/PollingObserver)
pandas==2.2.3           # log CSV + wczytywanie arkusza
openpyxl==3.1.5         # silnik Excel dla pandas.read_excel()

# --- OPCJONALNE, ale przydatne ---
pyyaml==6.0.2           # jeśli używasz YAML (flaga --config z .yaml/.yml)
python-dotenv==1.0.1    # wczytywanie .env (sekrety, ścieżki) - opcjonalnie

# --- POWIADOMIENIA (jeśli wrócimy do nich później) ---
